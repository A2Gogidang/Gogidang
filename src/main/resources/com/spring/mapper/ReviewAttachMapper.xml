<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	 
<mapper namespace="com.spring.mapper.ReviewAttachMapper">
	
	<insert id="insert">
		insert into review_attach (uuid, uploadpath, filename, filetype, review_num)
		values (#{uuid}, #{uploadPath}, #{fileName}, #{fileType}, #{review_num} )
	</insert>

	<delete id="delete">
		delete from review_attach where uuid = #{uuid}
	</delete>

	<select id="findByBno" resultType="ReviewAttachVO">
		select * from review_attach where review_num = #{review_num}
	</select>

	<delete id="deleteAll">
		delete review_attach where review_num = #{review_num}
	</delete>

	<select id="getOldFiles" resultType="ReviewAttachVO">
		select * from review_attach where uploadpath = to_char(sysdate -1 ,'yyyy\mm\dd')
	</select>
</mapper>